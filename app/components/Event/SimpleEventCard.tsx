import React from 'react'

interface Props {
}
/**
 * This is the EventCard that has no images and will be used on the event timeline and the map
 * DEVELOP:
 */

const SimpleEventCard: React.FC<Props> = (props: Props) => {
    return (
        <div>
            DetailedCard
        </div>
    )
}

export default SimpleEventCard
