import React from 'react'

interface Props {

}
/**
 * This is the sidebar that is always on the left side of the screen 
 * and links to the other pages
 * DEVELOP:
 */
const StaticSidebar: React.FC<Props> = (props) => {
    return (
        <div>
            StaticSidebar
        </div>
    )
}

export default StaticSidebar
